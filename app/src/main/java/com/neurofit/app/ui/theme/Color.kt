package com.neurofit.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * NEUROFIT palette. Dark is the only theme.
 * Neon is for strokes, glows, data and small accents. Never a large fill,
 * never body text at small sizes.
 */
object NeuroColors {
    val BackgroundVoid = Color(0xFF04060B)
    val SurfaceDeep = Color(0xFF0A0E18)
    val SurfaceRaised = Color(0xFF101725)
    val GridLine = Color(0xFF1A2438)
    val BorderDim = Color(0xFF223049)

    val NeonCyan = Color(0xFF00E5FF)
    val NeonMagenta = Color(0xFFFF2D95)
    val AcidGreen = Color(0xFFA3FF12)
    val Amber = Color(0xFFFFB020)
    val Violet = Color(0xFF7B2CFF)
    val DangerRed = Color(0xFFFF3B3B)

    val TextPrimary = Color(0xFFE6F7FF)
    val TextSecondary = Color(0xFF8A9BB5)
    val TextDim = Color(0xFF4F5F78)
}
