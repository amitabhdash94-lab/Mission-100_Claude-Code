# Copy to keystore.properties (which is gitignored) only if you build locally.
# CI does NOT use this file; it uses environment variables from repository secrets.
storeFile=keystore/release.jks
storePassword=CHANGE_ME
keyAlias=neurofit
keyPassword=CHANGE_ME
